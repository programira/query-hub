import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#002A3A", // Deep Navy Blue from the logo
      contrastText: "#FFFFFF",
    },
    secondary: {
      main: "#E600C9", // Pink from the logo
    },
  },
});
